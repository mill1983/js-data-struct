export class TreeNode {
    p1: Person
    leftChild: Node
    rightChild: Node
}

export class Person {
    iData: number;
    fData: number;
}

export class Tree {
    private root: TreeNode
    public find(key: number): void {
        let current = this.root
    }
    public insert(id: number, dd: number): void {

    }
    public delete(id: number): void {

    }
}